apply from: '../library.gradle'

dependencies {
    // support library
    implementation "com.android.support:support-v4:$SUPPORT_ARTIFACT_VERSION"

    // design project
    implementation project(':libs:design')
}
