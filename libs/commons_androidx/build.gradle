apply from: '../library.gradle'
apply from: '../testAndroidX.gradle'

dependencies {
    // androidX
    api "androidx.appcompat:appcompat:$ANDROIDX_VERSION"
    api "androidx.lifecycle:lifecycle-extensions:$ANDROIDX_LIFECYCLE_VERSION"

    // commons project
    api project(':libs:commons')
}
